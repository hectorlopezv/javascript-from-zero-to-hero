const pr = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    dispoible: true,
    test_: {
        papel: "momia"
    }
}

console.log(Object.keys(pr) );

console.log(Object.values(pr) );

console.log(Object.entries(pr) );

 