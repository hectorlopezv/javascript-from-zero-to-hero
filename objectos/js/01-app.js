const nombre = "Monitor 20 pulgadas";
const precio = 300 ;
const dispoible = true;


// un obecto agrupa todo en una sola variable

//objecto literal
const pr = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    dispoible: true,
}

console.log(pr);

